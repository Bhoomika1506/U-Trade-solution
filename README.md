# U-Trade-solution
A thread-safe in-memory key-value store built in C++ with TTL expiration, persistence snapshots, TCP/stdin support, and Redis-like commands such as SET, GET, DEL, KEYS, INCR, list operations, and Pub/Sub. Designed for caching, session management, and concurrent access.
